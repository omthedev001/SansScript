expr : term((PLUS|MINUS)term)
       KEYWORD: CHARAH IDENTIFIER COLON expr
term : factor((MULTIPLY|DIVIDE)factor)
factor : (PLUS|MINUS) factor
power : atom (POW factor)
atom : INT/FLOAT
       LPAREN expr RPAREN